# Default line endings for source and binary files
* text=auto eol=lf
*.png binary

# People downloading a ZIP of the repository don't need these files
# because they are either for development purposes or just for the
# git repository.
/.ci export-ignore
/.git export-ignore
/.gitattributes export-ignore
/.gitignore export-ignore
/.travis.yml export-ignore
